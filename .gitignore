# Xcode build artifacts
build/
DerivedData/
*.xcodeproj
*.xcworkspace
xcuserdata/
*.xcuserstate

# XcodeGen regenerates .xcodeproj from project.yml each run, so the
# generated project is intentionally ignored. Local devs run
# `xcodegen generate` before opening Xcode.

# Apple Developer artifacts â€” never commit
*.p8
AuthKey_*.p8
*.mobileprovision
*.cer
*.p12

# OS / IDE noise
.DS_Store
*.swp
Thumbs.db

# Local saved pages / personal files
*Claude_files/
*.html.ÏÂÔØ
Gemini_Generated_*.png
ce88724a-*.png
_*.png
